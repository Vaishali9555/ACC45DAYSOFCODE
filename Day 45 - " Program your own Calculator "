#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	double a,b;
	cin>>a>>b;
	string c;
	cin>>c;
	if (c=="+")
	cout<<a+b<<endl;
	else if(c=="-")
	cout<<a-b<<endl;
	else if (c=="*")
	cout<<a*b<<endl;
	else if(c=="/")
	cout<<fixed<<(a/b)<<endl;
	
	return 0;
}
